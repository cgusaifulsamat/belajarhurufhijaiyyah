<!DOCTYPE html>
<html lang="ms">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Belajar Huruf Hijaiyyah</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Poppins:wght@300;400;600;700&display=swap');
        
        .arabic-font { font-family: 'Amiri', serif; }
        .malay-font { font-family: 'Poppins', sans-serif; }
        
        .tab-active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .hijaiyyah-card {
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        }
        
        .hijaiyyah-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        
        .drawing-canvas {
            border: 3px dashed #e2e8f0;
            background: #f8fafc;
        }
        
        .coloring-area {
            cursor: pointer;
            transition: all 0.2s ease;
        }
        
        .coloring-area:hover {
            transform: scale(1.05);
        }
        
        .match-card {
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .match-card:hover {
            transform: scale(1.05);
            background: #f3f4f6 !important;
            border-color: #9ca3af !important;
        }
        
        .match-selected {
            border: 3px solid #f59e0b;
            background: #fef3c7;
        }
        
        .match-selected:hover {
            background: #fef3c7 !important;
            border-color: #f59e0b !important;
        }
        
        .match-wrong {
            border: 3px solid #dc2626;
            background: #fecaca;
            animation: shake 0.5s ease-in-out;
        }
        
        .match-wrong:hover {
            background: #fecaca !important;
            border-color: #dc2626 !important;
        }
        
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }
        
        @keyframes bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        
        .match-correct {
            border: 3px solid #2563eb;
            background: #dbeafe;
            animation: bounce 0.6s ease-in-out;
        }
        
        .match-correct:hover {
            background: #dbeafe !important;
            border-color: #2563eb !important;
        }
        
        .match-completed {
            border: 3px solid #9ca3af;
            background: #f3f4f6;
            opacity: 0.7;
        }
        
        .match-completed:hover {
            background: #f3f4f6 !important;
            border-color: #9ca3af !important;
            transform: none !important;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-purple-50 min-h-screen malay-font">
    <div class="w-full px-2 py-4 md:container md:mx-auto md:px-4 md:py-6">
        <!-- Header -->
        <div class="text-center mb-6 md:mb-8">
            <h1 class="text-2xl md:text-4xl font-bold text-gray-800 mb-2">🕌 Belajar Huruf Hijaiyyah</h1>
            <p class="text-sm md:text-base text-gray-600 px-2">Mari belajar huruf Arab dengan cara yang menyeronokkan!</p>
        </div>

        <!-- Tab Navigation -->
        <div class="flex flex-wrap justify-center mb-6 md:mb-8 bg-white rounded-xl shadow-lg p-1 md:p-2 mx-1">
            <button onclick="showTab('pronunciation')" id="tab-pronunciation" class="tab-active px-3 md:px-6 py-2 md:py-3 rounded-lg font-semibold mx-0.5 mb-1 md:mb-2 transition-all text-xs md:text-base">
                🔊 Sebutan
            </button>
            <button onclick="showTab('writing')" id="tab-writing" class="bg-gray-100 text-gray-700 px-3 md:px-6 py-2 md:py-3 rounded-lg font-semibold mx-0.5 mb-1 md:mb-2 transition-all hover:bg-gray-200 text-xs md:text-base">
                ✍️ Menulis
            </button>
            <button onclick="showTab('coloring')" id="tab-coloring" class="bg-gray-100 text-gray-700 px-3 md:px-6 py-2 md:py-3 rounded-lg font-semibold mx-0.5 mb-1 md:mb-2 transition-all hover:bg-gray-200 text-xs md:text-base">
                🎨 Mewarna
            </button>
            <button onclick="showTab('matching')" id="tab-matching" class="bg-gray-100 text-gray-700 px-3 md:px-6 py-2 md:py-3 rounded-lg font-semibold mx-0.5 mb-1 md:mb-2 transition-all hover:bg-gray-200 text-xs md:text-base">
                🔗 Padanan
            </button>
        </div>

        <!-- Tab 1: Pronunciation -->
        <div id="pronunciation-tab" class="tab-content">
            <div class="bg-white rounded-xl shadow-lg p-3 md:p-6 mb-4 md:mb-6 mx-1">
                <h2 class="text-lg md:text-2xl font-bold text-center mb-4 md:mb-6 text-gray-800">Belajar Sebutan Huruf Hijaiyyah</h2>
                <div class="grid grid-cols-2 gap-3 md:gap-4" id="pronunciation-grid">
                    <!-- Huruf akan diisi oleh JavaScript -->
                </div>
            </div>
        </div>

        <!-- Tab 2: Writing -->
        <div id="writing-tab" class="tab-content hidden">
            <div class="bg-white rounded-xl shadow-lg p-3 md:p-6 mx-1">
                <h2 class="text-lg md:text-2xl font-bold text-center mb-4 md:mb-6 text-gray-800">Latihan Menulis Huruf</h2>
                <div class="flex flex-col lg:flex-row gap-4 md:gap-6">
                    <div class="lg:w-1/3">
                        <h3 class="text-base md:text-lg font-semibold mb-3 md:mb-4">Pilih Huruf:</h3>
                        <div class="grid grid-cols-7 gap-1 md:gap-2 max-h-48 md:max-h-96 overflow-y-auto" id="writing-selector">
                            <!-- Huruf selector akan diisi oleh JavaScript -->
                        </div>
                    </div>
                    <div class="lg:w-2/3">
                        <div class="text-center mb-3 md:mb-4">
                            <div id="current-letter" class="text-6xl md:text-8xl arabic-font text-purple-600 mb-2">ا</div>
                            <div id="current-letter-name" class="text-lg md:text-xl font-semibold text-gray-700 mb-1">Alif</div>
                            <div id="current-letter-sound" class="text-base md:text-lg text-blue-600 mb-2">Sebutan: "a"</div>
                            <p class="text-sm md:text-base text-gray-600 px-2">Tulis huruf di atas pada kanvas di bawah</p>
                        </div>
                        <canvas id="writing-canvas" class="drawing-canvas w-full h-48 md:h-64 rounded-lg mx-auto block"></canvas>
                        <div class="text-center mt-3 md:mt-4">
                            <button onclick="clearCanvas()" class="bg-red-500 hover:bg-red-600 text-white px-4 md:px-6 py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                                Padam
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 3: Coloring -->
        <div id="coloring-tab" class="tab-content hidden">
            <div class="bg-white rounded-xl shadow-lg p-3 md:p-6 mx-1">
                <h2 class="text-lg md:text-2xl font-bold text-center mb-4 md:mb-6 text-gray-800">Mewarna dan Meneka Huruf</h2>
                <div class="flex flex-col lg:flex-row gap-4 md:gap-6">
                    <div class="lg:w-1/2">
                        <h3 class="text-base md:text-lg font-semibold mb-3 md:mb-4">Warnakan huruf ini:</h3>
                        <div class="text-center">
                            <svg id="coloring-svg" width="160" height="160" class="mx-auto border-2 border-gray-300 rounded-lg md:w-48 md:h-48">
                                <text x="80" y="100" font-size="100" font-family="Amiri" text-anchor="middle" fill="white" stroke="#333" stroke-width="2" class="coloring-area md:text-6xl">ا</text>
                            </svg>
                        </div>
                        <p id="color-instruction" class="text-xs md:text-sm text-gray-600 text-center mb-2">Pilih warna <span class="font-semibold text-red-500">MERAH</span> untuk mewarnakan huruf:</p>
                        <div class="flex justify-center mt-3 md:mt-4 space-x-1 md:space-x-2">
                            <button onclick="colorLetter('#ff6b6b')" class="w-6 h-6 md:w-8 md:h-8 bg-red-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Merah"></button>
                            <button onclick="colorLetter('#4ecdc4')" class="w-6 h-6 md:w-8 md:h-8 bg-teal-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Hijau Laut"></button>
                            <button onclick="colorLetter('#45b7d1')" class="w-6 h-6 md:w-8 md:h-8 bg-blue-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Biru"></button>
                            <button onclick="colorLetter('#96ceb4')" class="w-6 h-6 md:w-8 md:h-8 bg-green-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Hijau"></button>
                            <button onclick="colorLetter('#feca57')" class="w-6 h-6 md:w-8 md:h-8 bg-yellow-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Kuning"></button>
                            <button onclick="colorLetter('#ff9ff3')" class="w-6 h-6 md:w-8 md:h-8 bg-pink-400 rounded-full border-2 border-gray-300 hover:scale-110 transition-all" title="Merah Jambu"></button>
                        </div>
                    </div>
                    <div class="lg:w-1/2">
                        <h3 class="text-base md:text-lg font-semibold mb-3 md:mb-4">Apakah huruf ini?</h3>
                        <div class="grid grid-cols-2 gap-2 md:gap-3" id="guess-options">
                            <!-- Pilihan akan diisi oleh JavaScript -->
                        </div>
                        <div id="coloring-feedback" class="mt-3 md:mt-4 text-center font-semibold text-sm md:text-base"></div>
                        <button onclick="nextColoringQuestion()" class="w-full mt-3 md:mt-4 bg-purple-500 hover:bg-purple-600 text-white py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                            Soalan Seterusnya
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 4: Matching -->
        <div id="matching-tab" class="tab-content hidden">
            <div class="bg-white rounded-xl shadow-lg p-3 md:p-6 mx-1">
                <h2 class="text-lg md:text-2xl font-bold text-center mb-4 md:mb-6 text-gray-800">Padankan Huruf dengan Gambar</h2>
                <div class="text-center mb-3 md:mb-4">
                    <p class="text-sm md:text-base text-gray-600 px-2">Klik huruf, kemudian klik gambar yang sesuai</p>
                    <div class="mt-2 space-x-2">
                        <span class="text-xs md:text-sm bg-purple-100 text-purple-800 px-2 md:px-3 py-1 rounded-full">Level: <span id="current-level">1</span></span>
                        <span class="text-xs md:text-sm bg-blue-100 text-blue-800 px-2 md:px-3 py-1 rounded-full">Skor: <span id="match-score">0</span></span>
                    </div>
                </div>
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 md:gap-8">
                    <div>
                        <h3 class="text-base md:text-lg font-semibold mb-3 md:mb-4 text-center">Huruf:</h3>
                        <div class="grid grid-cols-2 gap-2 md:gap-4" id="letters-to-match">
                            <!-- Huruf akan diisi oleh JavaScript -->
                        </div>
                    </div>
                    <div>
                        <h3 class="text-base md:text-lg font-semibold mb-3 md:mb-4 text-center">Gambar:</h3>
                        <div class="grid grid-cols-2 gap-2 md:gap-4" id="images-to-match">
                            <!-- Gambar akan diisi oleh JavaScript -->
                        </div>
                    </div>
                </div>
                <div class="text-center mt-4 md:mt-6">
                    <div id="level-complete-message" class="hidden mb-4 p-4 bg-green-100 border border-green-300 rounded-lg">
                        <p class="text-green-800 font-semibold text-lg">🎉 Tahniah! Level <span id="completed-level">1</span> selesai!</p>
                        <p class="text-green-700 text-sm mt-1">Skor anda: <span id="level-score">40</span> mata</p>
                    </div>
                    <div id="hint-message" class="hidden mb-4 p-3 bg-yellow-100 border border-yellow-300 rounded-lg">
                        <p class="text-yellow-800 font-semibold text-sm">💡 Hint: <span id="hint-text"></span></p>
                    </div>
                    <div class="space-x-2">
                        <button onclick="resetMatching()" class="bg-green-500 hover:bg-green-600 text-white px-4 md:px-6 py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                            Mula Semula
                        </button>
                        <button id="hint-btn" onclick="showHint()" class="bg-yellow-500 hover:bg-yellow-600 text-white px-4 md:px-6 py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                            💡 Hint (-10 mata)
                        </button>
                        <button id="next-level-btn" onclick="startNextLevel()" class="hidden bg-blue-500 hover:bg-blue-600 text-white px-4 md:px-6 py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                            Level Seterusnya
                        </button>
                        <button id="final-message-btn" class="hidden bg-purple-500 hover:bg-purple-600 text-white px-4 md:px-6 py-2 rounded-lg font-semibold transition-all text-sm md:text-base">
                            🏆 Anda Juara!
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Data huruf Hijaiyyah
        const hijaiyyahLetters = [
            {letter: 'ا', name: 'Alif', sound: 'a', image: '🦁', word: 'أسد (Asad - Singa)'},
            {letter: 'ب', name: 'Ba', sound: 'ba', image: '🏠', word: 'بيت (Bait - Rumah)'},
            {letter: 'ت', name: 'Ta', sound: 'ta', image: '🍎', word: 'تفاح (Tuffah - Epal)'},
            {letter: 'ث', name: 'Tsa', sound: 'tsa', image: '🐍', word: 'ثعبان (Tsu\'ban - Ular)'},
            {letter: 'ج', name: 'Jim', sound: 'ja', image: '🐪', word: 'جمل (Jamal - Unta)'},
            {letter: 'ح', name: 'Ha', sound: 'ha', image: '🐴', word: 'حصان (Hisan - Kuda)'},
            {letter: 'خ', name: 'Kha', sound: 'kha', image: '🥒', word: 'خيار (Khiyar - Timun)'},
            {letter: 'د', name: 'Dal', sound: 'da', image: '🐻', word: 'دب (Dubb - Beruang)'},
            {letter: 'ذ', name: 'Dzal', sound: 'dza', image: '🐺', word: 'ذئب (Dzi\'b - serigala)'},
            {letter: 'ر', name: 'Ra', sound: 'ra', image: '👨', word: 'رجل (Rajul - Lelaki)'},
            {letter: 'ز', name: 'Zai', sound: 'za', image: '🌸', word: 'زهرة (Zahrah - Bunga)'},
            {letter: 'س', name: 'Sin', sound: 'sa', image: '🐟', word: 'سمك (Samak - Ikan)'},
            {letter: 'ش', name: 'Syin', sound: 'sya', image: '☀️', word: 'شمس (Syams - Matahari)'},
            {letter: 'ص', name: 'Sad', sound: 'sa', image: '📦', word: 'صندوق (Sanduq - Kotak)'},
            {letter: 'ض', name: 'Dad', sound: 'da', image: '🐸', word: 'ضفدع (Dafda\' - Katak)'},
            {letter: 'ط', name: 'Ta', sound: 'ta', image: '🐦', word: 'طير (Tair - Burung)'},
            {letter: 'ظ', name: 'Za', sound: 'za', image: '💅', word: 'ظفر (Zufr - Kuku)'},
            {letter: 'ع', name: 'Ain', sound: 'a', image: '👁️', word: 'عين (Ain - Mata)'},
            {letter: 'غ', name: 'Ghain', sound: 'gha', image: '☁️', word: 'غيم (Ghaim - Awan)'},
            {letter: 'ف', name: 'Fa', sound: 'fa', image: '🐭', word: 'فأر (Fa\'r - Tikus)'},
            {letter: 'ق', name: 'Qaf', sound: 'qa', image: '🐒', word: 'قرد (Qird - Monyet)'},
            {letter: 'ك', name: 'Kaf', sound: 'ka', image: '📖', word: 'كتاب (Kitab - Buku)'},
            {letter: 'ل', name: 'Lam', sound: 'la', image: '🥛', word: 'لبن (Laban - Susu)'},
            {letter: 'م', name: 'Mim', sound: 'ma', image: '💧', word: 'ماء (Ma\' - Air)'},
            {letter: 'ن', name: 'Nun', sound: 'na', image: '🔥', word: 'نار (Nar - Api)'},
            {letter: 'ه', name: 'Ha', sound: 'ha', image: '🎁', word: 'هدية (Hadiyyah - Hadiah)'},
            {letter: 'و', name: 'Wau', sound: 'wa', image: '🌹', word: 'وردة (Wardah - Mawar)'},
            {letter: 'ي', name: 'Ya', sound: 'ya', image: '✋', word: 'يد (Yad - Tangan)'}
        ];

        let currentWritingLetter = 0;
        let currentColoringLetter = 0;
        let selectedMatchLetter = null;
        let matchScore = 0;
        let matchedPairs = [];
        let currentLevel = 1;

        // Tab switching
        function showTab(tabName) {
            // Hide all tabs
            document.querySelectorAll('.tab-content').forEach(tab => {
                tab.classList.add('hidden');
            });
            
            // Remove active class from all tab buttons
            document.querySelectorAll('button[id^="tab-"]').forEach(btn => {
                btn.classList.remove('tab-active');
                btn.classList.add('bg-gray-100', 'text-gray-700');
            });
            
            // Show selected tab
            document.getElementById(tabName + '-tab').classList.remove('hidden');
            
            // Add active class to selected tab button
            const activeBtn = document.getElementById('tab-' + tabName);
            activeBtn.classList.add('tab-active');
            activeBtn.classList.remove('bg-gray-100', 'text-gray-700');
        }

        // Initialize pronunciation tab
        function initPronunciationTab() {
            const grid = document.getElementById('pronunciation-grid');
            grid.innerHTML = '';
            
            hijaiyyahLetters.forEach((item, index) => {
                const card = document.createElement('div');
                card.className = 'hijaiyyah-card text-white p-4 rounded-xl text-center cursor-pointer';
                card.innerHTML = `
                    <div class="text-2xl md:text-4xl arabic-font mb-1 md:mb-2">${item.letter}</div>
                    <div class="text-xs md:text-sm font-semibold">${item.name}</div>
                    <div class="text-xs mt-1 hidden md:block">${item.sound}</div>
                `;
                card.onclick = () => pronounceLetter(item);
                grid.appendChild(card);
            });
        }

        function pronounceLetter(item) {
            // Simulate pronunciation (in real app, you'd use Web Speech API or audio files)
            const utterance = new SpeechSynthesisUtterance(item.sound);
            utterance.lang = 'ar-SA';
            utterance.rate = 0.7;
            speechSynthesis.speak(utterance);
            
            // Show word example
            alert(`${item.letter} - ${item.name}\nContoh: ${item.word}`);
        }

        // Initialize writing tab
        function initWritingTab() {
            const selector = document.getElementById('writing-selector');
            selector.innerHTML = '';
            
            hijaiyyahLetters.forEach((item, index) => {
                const btn = document.createElement('button');
                btn.className = 'bg-purple-100 hover:bg-purple-200 text-purple-800 p-1 md:p-2 rounded-lg text-lg md:text-2xl arabic-font transition-all';
                btn.textContent = item.letter;
                btn.onclick = () => selectWritingLetter(item.letter, item.name);
                selector.appendChild(btn);
            });
            
            initCanvas();
        }

        function selectWritingLetter(letter, name) {
            const selectedLetter = hijaiyyahLetters.find(item => item.letter === letter);
            document.getElementById('current-letter').textContent = letter;
            document.getElementById('current-letter-name').textContent = selectedLetter.name;
            document.getElementById('current-letter-sound').textContent = `Sebutan: "${selectedLetter.sound}"`;
            clearCanvas();
        }

        function initCanvas() {
            const canvas = document.getElementById('writing-canvas');
            const ctx = canvas.getContext('2d');
            let isDrawing = false;

            // Set fixed canvas size
            canvas.width = 600;
            canvas.height = 300;

            canvas.addEventListener('mousedown', startDrawing);
            canvas.addEventListener('mousemove', draw);
            canvas.addEventListener('mouseup', stopDrawing);
            canvas.addEventListener('mouseout', stopDrawing);
            canvas.addEventListener('touchstart', handleTouch);
            canvas.addEventListener('touchmove', handleTouch);
            canvas.addEventListener('touchend', stopDrawing);

            function getMousePos(e) {
                const rect = canvas.getBoundingClientRect();
                const scaleX = canvas.width / rect.width;
                const scaleY = canvas.height / rect.height;
                
                return {
                    x: (e.clientX - rect.left) * scaleX,
                    y: (e.clientY - rect.top) * scaleY
                };
            }

            function startDrawing(e) {
                isDrawing = true;
                const pos = getMousePos(e);
                
                ctx.beginPath();
                ctx.moveTo(pos.x, pos.y);
                ctx.lineWidth = 4;
                ctx.lineCap = 'round';
                ctx.strokeStyle = '#7c3aed';
            }

            function draw(e) {
                if (!isDrawing) return;
                
                const pos = getMousePos(e);
                ctx.lineTo(pos.x, pos.y);
                ctx.stroke();
            }

            function stopDrawing() {
                if (isDrawing) {
                    isDrawing = false;
                    ctx.beginPath();
                }
            }

            function handleTouch(e) {
                e.preventDefault();
                const touch = e.touches[0];
                const rect = canvas.getBoundingClientRect();
                const scaleX = canvas.width / rect.width;
                const scaleY = canvas.height / rect.height;
                
                const pos = {
                    x: (touch.clientX - rect.left) * scaleX,
                    y: (touch.clientY - rect.top) * scaleY
                };
                
                if (e.type === 'touchstart') {
                    isDrawing = true;
                    ctx.beginPath();
                    ctx.moveTo(pos.x, pos.y);
                    ctx.lineWidth = 4;
                    ctx.lineCap = 'round';
                    ctx.strokeStyle = '#7c3aed';
                } else if (e.type === 'touchmove' && isDrawing) {
                    ctx.lineTo(pos.x, pos.y);
                    ctx.stroke();
                }
            }
        }

        function clearCanvas() {
            const canvas = document.getElementById('writing-canvas');
            const ctx = canvas.getContext('2d');
            ctx.clearRect(0, 0, canvas.width, canvas.height);
        }

        // Initialize coloring tab
        function initColoringTab() {
            nextColoringQuestion();
        }

        function colorLetter(color) {
            const svg = document.querySelector('#coloring-svg text');
            svg.setAttribute('fill', color);
            
            // Check if correct color was selected
            if (color === targetColor) {
                isColorCorrect = true;
                const feedback = document.getElementById('coloring-feedback');
                feedback.textContent = `✅ Bagus! Anda telah memilih warna ${targetColorName} yang betul!`;
                feedback.className = 'mt-4 text-center font-semibold text-green-600';
            } else {
                isColorCorrect = false;
                const feedback = document.getElementById('coloring-feedback');
                feedback.textContent = `❌ Warna salah! Sila pilih warna ${targetColorName}.`;
                feedback.className = 'mt-4 text-center font-semibold text-red-600';
            }
        }

        let targetColor = '';
        let targetColorName = '';
        let isColorCorrect = false;

        function nextColoringQuestion() {
            currentColoringLetter = Math.floor(Math.random() * hijaiyyahLetters.length);
            const currentLetter = hijaiyyahLetters[currentColoringLetter];
            
            // Reset color state
            isColorCorrect = false;
            
            // Select random target color
            const colors = [
                {color: '#ff6b6b', name: 'MERAH', class: 'text-red-500'},
                {color: '#4ecdc4', name: 'HIJAU LAUT', class: 'text-teal-500'},
                {color: '#45b7d1', name: 'BIRU', class: 'text-blue-500'},
                {color: '#96ceb4', name: 'HIJAU', class: 'text-green-500'},
                {color: '#feca57', name: 'KUNING', class: 'text-yellow-500'},
                {color: '#ff9ff3', name: 'MERAH JAMBU', class: 'text-pink-500'}
            ];
            
            const randomColor = colors[Math.floor(Math.random() * colors.length)];
            targetColor = randomColor.color;
            targetColorName = randomColor.name;
            
            // Update instruction
            const instruction = document.getElementById('color-instruction');
            instruction.innerHTML = `Pilih warna <span class="font-semibold ${randomColor.class}">${randomColor.name}</span> untuk mewarnakan huruf:`;
            
            // Update SVG
            const svg = document.querySelector('#coloring-svg text');
            svg.textContent = currentLetter.letter;
            svg.setAttribute('fill', 'white');
            
            // Create options
            const options = document.getElementById('guess-options');
            options.innerHTML = '';
            
            // Get 3 random wrong answers + 1 correct answer
            const wrongAnswers = hijaiyyahLetters
                .filter((_, index) => index !== currentColoringLetter)
                .sort(() => Math.random() - 0.5)
                .slice(0, 3);
            
            const allOptions = [...wrongAnswers, currentLetter].sort(() => Math.random() - 0.5);
            
            allOptions.forEach(option => {
                const btn = document.createElement('button');
                btn.className = 'bg-blue-100 hover:bg-blue-200 text-blue-800 p-2 md:p-3 rounded-lg font-semibold transition-all text-sm md:text-base';
                btn.textContent = option.name;
                btn.onclick = () => checkColoringAnswer(option.name === currentLetter.name);
                options.appendChild(btn);
            });
            
            document.getElementById('coloring-feedback').textContent = '';
        }

        function checkColoringAnswer(isCorrect) {
            const feedback = document.getElementById('coloring-feedback');
            
            if (!isColorCorrect) {
                feedback.textContent = `❌ Sila warnakan huruf dengan warna ${targetColorName} dahulu!`;
                feedback.className = 'mt-4 text-center font-semibold text-orange-600';
                return;
            }
            
            if (isCorrect) {
                feedback.textContent = '🎉 Sempurna! Warna betul dan huruf betul!';
                feedback.className = 'mt-4 text-center font-semibold text-green-600';
            } else {
                feedback.textContent = '❌ Warna betul, tetapi huruf ini ialah ' + hijaiyyahLetters[currentColoringLetter].name;
                feedback.className = 'mt-4 text-center font-semibold text-red-600';
            }
        }

        // Initialize matching tab
        function initMatchingTab() {
            resetMatching();
        }

        function resetMatching() {
            matchScore = 0;
            matchedPairs = [];
            selectedMatchLetter = null;
            currentLevel = 1;
            document.getElementById('match-score').textContent = matchScore;
            document.getElementById('current-level').textContent = currentLevel;
            
            // Hide completion message and buttons
            document.getElementById('level-complete-message').classList.add('hidden');
            document.getElementById('next-level-btn').classList.add('hidden');
            document.getElementById('final-message-btn').classList.add('hidden');
            document.getElementById('hint-message').classList.add('hidden');
            document.getElementById('hint-btn').classList.remove('hidden');
            
            // Select 4 random letters for matching
            const selectedLetters = hijaiyyahLetters
                .sort(() => Math.random() - 0.5)
                .slice(0, 4);
            
            // Create letter cards
            const lettersContainer = document.getElementById('letters-to-match');
            lettersContainer.innerHTML = '';
            
            selectedLetters.forEach((item, index) => {
                const card = document.createElement('div');
                card.className = 'match-card bg-blue-100 p-4 rounded-xl text-center border-2 border-transparent';
                card.innerHTML = `
                    <div class="text-3xl md:text-4xl arabic-font mb-1 md:mb-2">${item.letter}</div>
                    <div class="text-xs md:text-sm font-semibold">${item.name}</div>
                `;
                card.onclick = () => selectLetter(index, card);
                card.dataset.index = index;
                lettersContainer.appendChild(card);
            });
            
            // Create image cards (shuffled)
            const imagesContainer = document.getElementById('images-to-match');
            imagesContainer.innerHTML = '';
            
            const shuffledImages = [...selectedLetters].sort(() => Math.random() - 0.5);
            
            shuffledImages.forEach((item, imageIndex) => {
                const card = document.createElement('div');
                card.className = 'match-card bg-green-100 p-4 rounded-xl text-center border-2 border-transparent';
                card.innerHTML = `
                    <div class="text-3xl md:text-4xl mb-1 md:mb-2">${item.image}</div>
                    <div class="text-xs md:text-sm font-semibold">${item.word.split(' ')[0]}</div>
                `;
                const originalIndex = selectedLetters.findIndex(l => l.letter === item.letter);
                card.onclick = () => selectImage(originalIndex, card);
                card.dataset.index = originalIndex;
                imagesContainer.appendChild(card);
            });
            
            window.selectedLetters = selectedLetters;
        }

        function selectLetter(index, card) {
            if (matchedPairs.includes(index)) return;
            
            // Remove previous selection
            document.querySelectorAll('#letters-to-match .match-card').forEach(c => {
                c.classList.remove('match-selected');
            });
            
            selectedMatchLetter = index;
            card.classList.add('match-selected');
        }

        function selectImage(index, card) {
            if (matchedPairs.includes(index) || selectedMatchLetter === null) return;
            
            if (selectedMatchLetter === index) {
                // Correct match
                card.classList.add('match-correct');
                document.querySelector(`#letters-to-match .match-card[data-index="${index}"]`).classList.add('match-correct');
                matchedPairs.push(index);
                matchScore += 10;
                document.getElementById('match-score').textContent = matchScore;
                selectedMatchLetter = null;
                
                console.log('Pasangan betul! Skor sekarang:', matchScore); // Debug log
                console.log('Skor untuk level ini:', (currentLevel - 1) * 40 + 40); // Debug log
                
                // Check level completion based on matched pairs (4 pairs per level)
                if (matchedPairs.length === 4) {
                    console.log('Level selesai! Skor mencapai:', matchScore); // Debug log
                    
                    // Show completion message and next level button
                    setTimeout(() => {
                        const messageDiv = document.getElementById('level-complete-message');
                        const completedLevelSpan = document.getElementById('completed-level');
                        const levelScoreSpan = document.getElementById('level-score');
                        const nextLevelBtn = document.getElementById('next-level-btn');
                        const finalMessageBtn = document.getElementById('final-message-btn');
                        
                        completedLevelSpan.textContent = currentLevel;
                        levelScoreSpan.textContent = matchScore;
                        messageDiv.classList.remove('hidden');
                        
                        if (currentLevel < 5) {
                            nextLevelBtn.classList.remove('hidden');
                        } else {
                            finalMessageBtn.classList.remove('hidden');
                            messageDiv.innerHTML = `
                                <p class="text-purple-800 font-semibold text-xl">🏆 TAHNIAH! Anda telah berjaya siapkan semua 5 level!</p>
                                <p class="text-purple-700 text-base mt-1">Skor akhir: ${matchScore} mata</p>
                                <p class="text-purple-600 text-sm mt-1">Anda adalah juara Hijaiyyah! 🎉</p>
                            `;
                            messageDiv.className = 'mb-4 p-4 bg-purple-100 border border-purple-300 rounded-lg';
                        }
                    }, 1000);
                }
                
                // After animation, change to gray
                setTimeout(() => {
                    card.classList.remove('match-correct');
                    card.classList.add('match-completed');
                    document.querySelector(`#letters-to-match .match-card[data-index="${index}"]`).classList.remove('match-correct');
                    document.querySelector(`#letters-to-match .match-card[data-index="${index}"]`).classList.add('match-completed');
                }, 600);
                
            } else {
                // Wrong match
                card.classList.add('match-wrong');
                setTimeout(() => {
                    card.classList.remove('match-wrong');
                }, 1000);
            }
        }

        function startNextLevel() {
            currentLevel++;
            matchedPairs = [];
            selectedMatchLetter = null;
            document.getElementById('current-level').textContent = currentLevel;
            
            // Hide completion message and buttons
            document.getElementById('level-complete-message').classList.add('hidden');
            document.getElementById('next-level-btn').classList.add('hidden');
            document.getElementById('final-message-btn').classList.add('hidden');
            document.getElementById('hint-message').classList.add('hidden');
            document.getElementById('hint-btn').classList.remove('hidden');
            
            // Select 4 random letters for matching
            const selectedLetters = hijaiyyahLetters
                .sort(() => Math.random() - 0.5)
                .slice(0, 4);
            
            // Create letter cards
            const lettersContainer = document.getElementById('letters-to-match');
            lettersContainer.innerHTML = '';
            
            selectedLetters.forEach((item, index) => {
                const card = document.createElement('div');
                card.className = 'match-card bg-blue-100 p-4 rounded-xl text-center border-2 border-transparent';
                card.innerHTML = `
                    <div class="text-3xl md:text-4xl arabic-font mb-1 md:mb-2">${item.letter}</div>
                    <div class="text-xs md:text-sm font-semibold">${item.name}</div>
                `;
                card.onclick = () => selectLetter(index, card);
                card.dataset.index = index;
                lettersContainer.appendChild(card);
            });
            
            // Create image cards (shuffled)
            const imagesContainer = document.getElementById('images-to-match');
            imagesContainer.innerHTML = '';
            
            const shuffledImages = [...selectedLetters].sort(() => Math.random() - 0.5);
            
            shuffledImages.forEach((item, imageIndex) => {
                const card = document.createElement('div');
                card.className = 'match-card bg-green-100 p-4 rounded-xl text-center border-2 border-transparent';
                card.innerHTML = `
                    <div class="text-3xl md:text-4xl mb-1 md:mb-2">${item.image}</div>
                    <div class="text-xs md:text-sm font-semibold">${item.word.split(' ')[0]}</div>
                `;
                const originalIndex = selectedLetters.findIndex(l => l.letter === item.letter);
                card.onclick = () => selectImage(originalIndex, card);
                card.dataset.index = originalIndex;
                imagesContainer.appendChild(card);
            });
            
            window.selectedLetters = selectedLetters;
        }

        function showHint() {
            // Check if there are unmatched pairs
            const unmatchedPairs = [];
            for (let i = 0; i < 4; i++) {
                if (!matchedPairs.includes(i)) {
                    unmatchedPairs.push(i);
                }
            }
            
            if (unmatchedPairs.length === 0) return;
            
            // Deduct 10 points for hint (but don't go below 0)
            matchScore = Math.max(0, matchScore - 10);
            document.getElementById('match-score').textContent = matchScore;
            
            // Show hint for a random unmatched pair
            const randomIndex = unmatchedPairs[Math.floor(Math.random() * unmatchedPairs.length)];
            const hintLetter = window.selectedLetters[randomIndex];
            
            // Display hint message
            const hintText = document.getElementById('hint-text');
            hintText.textContent = `Huruf "${hintLetter.letter}" (${hintLetter.name}) sepadan dengan ${hintLetter.image} - ${hintLetter.word}`;
            
            document.getElementById('hint-message').classList.remove('hidden');
            
            // Hide hint after 5 seconds
            setTimeout(() => {
                document.getElementById('hint-message').classList.add('hidden');
            }, 5000);
        }

        // Initialize all tabs
        document.addEventListener('DOMContentLoaded', function() {
            initPronunciationTab();
            initWritingTab();
            initColoringTab();
            initMatchingTab();
        });
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'97f6290fc154f543',t:'MTc1NzkxODcxNy4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
